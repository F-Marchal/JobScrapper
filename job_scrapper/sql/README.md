# SQL
This folder contains most class related to the use of SQL. 
1. `tables/` uses SQlAlchemy to define tables used inside the database
2. `filters/` uses `wrapper/` to build filters from user input 
3. `wrapper/` contain class that wraps SQLAlchemy methods / object in order
to help the `filters/` classes.
